# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'new_API_Countries' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for new_API_Countries
  pod 'Alamofire', '~> 4.7'

  target 'new_API_CountriesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'new_API_CountriesUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
